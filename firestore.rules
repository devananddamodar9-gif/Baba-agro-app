rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT ONLY.
    // Production मध्ये Firebase Authentication जोडून rules कडक करा.
    match /members/{memberId} {
      allow read, write: if false;
    }
  }
}
